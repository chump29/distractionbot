services:
  distractionbot:
    container_name: distractionbot
    image: git.postfmly.com/admin/distractionbot:arm64
    restart: unless-stopped
    ports:
      - "8006:8006"
    volumes:
      - db:/app/db
    environment:
      - LOGO_NAME=distractionbot.webp
      - LOGO_PATH=./utils/images
      - LOGO_PORT=8006
      - LOGO_URL=https://distractionbot.postfmly.us/distractionbot.webp
      - TZ=America/Chicago
    env_file: .env
    labels:
      - wud.display.name=DistractionBot
      - homepage.group=Personal
      - homepage.name=DistractionBot
      - homepage.icon=https://distractionbot.postfmly.com/distractionbot.webp

volumes:
  db:

networks: {}
